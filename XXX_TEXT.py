

git remote add origin https://github.com/dolby3d/GEMMAGTP.git

git init && git add . && git commit -m "Initial commit" && git branch -M main && git remote add origin https://github.com/dolby3d/GEMMAGTP.git && git push -u origin main




📌 Save a summary of what the script does (purpose, key libraries, features).
📎 Save a link/reference (if you put the code in a GitHub repo or paste site).
📝 Keep a short snippet (like a key function or critical section), if it’s reasonably small.

`load_script()`

(`load_script()`) trigger command

`update_fps()`.

`print_fps()`, 
`print_time()`,

`print_cpu_usage()`, 
`print_memory_usage()`,
`print_disk_usage()`, 
`print_network_usage()`,
`build_memory_bar()`,
`print_temperature()`,
`print_swap_usage()` 
 
`build_bar()`, 


mockup mockup mockup mockup mockup mockup mockup mockup mockup mockup 


load context 
load script-ascii mockup in chat
script ASCII mockup

CompactFormat: 
    GPU_ASCII_Capture
    Python,CuPy,NumPy,OpenCV,mss,psutil
    Modes:simple,gradient,ANSI256,RGB24
    |Features:brightness,contrast,snapshots,FPS,CPU_RAM_Battery
    |Goal:FPS_opt|Prefs:AllShortcutKeysAllwaysCheck,WeFixThanUpgrade,PrintScriptMockupBeforePrintingScriptsCodes-IfPosible
    |User: wants to know time via print_time()
    |Helpers: gpu_to_ascii,FPSCounter,build_bar,snapshot_monitor,print_time



|CompactFormat: something(something);
|Projects:something(something);
|Modes:something(something);
|Features:something(something);
|Preferences:something(something);
|User:something(something);
|Helpers:something(something);


|CompactFormat:
|Projects:;
|Modes: ;
|Features: ;
|Preferences: ;
|User:scripts(if fix=thrue(upgrad),time via print_time(), likes efficiency, clarity, precision;
|Helpers: ascii ,FPSCounter,build_bar,snapshot_monitor,print_time





